{
  "name": "apibuilder-swagger-generator",
  "version": "0.0.1",
  "description": "A Lambda function that translates from the Apibuilder service.json to a JSON document that adheres to Swagger Spec 2.0",
  "main": "app/handlers.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --recursive test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/apicollective/apibuilder-swagger-generator"
  },
  "keywords": [
    "apibuilder",
    "swagger"
  ],
  "author": "Ryan Martin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/apicollective/apibuilder-swagger-generator/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "aws-lambda-mock-context": "^1.1.0",
    "mocha": "^2.4.5"
  }
}
